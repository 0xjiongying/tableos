# Storybook

**Status:** Deferred for this session.

Core UI primitives live under `apps/web/src/components/ui` and are exercised by the live vertical slice + Playwright smoke.

When added, prefer a minimal Storybook 8 setup scoped to Button, Input, Card, Dialog, Table, Empty/Error — using the same CSS variables from `tokens.css`.
